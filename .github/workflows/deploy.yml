- name: Validate SSH_PRIVATE_KEY is set
  run: |
    if [ -z "${{ secrets.SSH_PRIVATE_KEY }}" ]; then
      echo "SSH_PRIVATE_KEY secret ist leer oder fehlt."
      exit 1
    else
      echo "SSH_PRIVATE_KEY ist gesetzt (Länge):"
      echo "${{ secrets.SSH_PRIVATE_KEY }}" | wc -c
    fi
