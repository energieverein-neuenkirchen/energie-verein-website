name: Deploy Website (rsync)

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Optional – nur wenn du ein Node-Frontend baust (Vite/Astro/Next)
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install & Build
        run: |
          if [ -f package.json ]; then
            npm ci || npm install
            npm run build || echo "Kein Build-Script gefunden"
          else
            mkdir -p dist
            cp -r * dist/ || true
          fi

      # 🔎 Secret-Prüfung (bricht sauber ab, wenn leer)
      - name: Validate SSH_PRIVATE_KEY is set
        run: |
          if [ -z "${{ secrets.SSH_PRIVATE_KEY }}" ]; then
            echo "SSH_PRI_
